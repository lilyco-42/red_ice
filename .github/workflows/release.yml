name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-linux:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            binary_name: red_ice-x86_64-linux
          - target: aarch64-unknown-linux-gnu
            binary_name: red_ice-arm64-linux
          - target: arm-unknown-linux-gnueabihf
            binary_name: red_ice-armv7-linux

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross
        run: cargo install cross

      - name: Build release
        run: cross build --release --manifest-path Cargo.toml -p red_ice --target ${{ matrix.target }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.binary_name }}
          path: target/${{ matrix.target }}/release/red_ice-cli
          retention-days: 5

  build-windows:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - target: x86_64-pc-windows-msvc
            binary_name: red_ice-x64-windows.exe
            zig_target: x86_64-windows
          - target: aarch64-pc-windows-msvc
            binary_name: red_ice-arm64-windows.exe
            zig_target: aarch64-windows

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install cargo-zigbuild
        run: cargo install cargo-zigbuild

      - name: Build release
        run: cargo zigbuild --release --manifest-path Cargo.toml -p red_ice --target ${{ matrix.zig_target }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.binary_name }}
          path: target/${{ matrix.zig_target }}/release/*/red_ice*
          retention-days: 5

  create-release:
    needs: [build-linux, build-windows]
    runs-on: ubuntu-latest
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}

    steps:
      - name: Create tag
        run: |
          TAG=${GITHUB_REF#refs/tags/}
          VERSION=${TAG#v}
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Create release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          draft: false
          prerelease: ${{ contains(github.ref, 'alpha') || contains(github.ref, 'beta') }}
          generate_release_notes: true

  upload-assets:
    needs: create-release
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - name: red_ice-x86_64-linux
          - name: red_ice-arm64-linux
          - name: red_ice-armv7-linux
          - name: red_ice-x64-windows.exe
          - name: red_ice-arm64-windows.exe

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ matrix.name }}
          path: dist

      - name: Upload to release
        uses: softprops/action-gh-release@v1
        with:
          upload_url: ${{ needs.create-release.outputs.upload_url }}
          file: dist/${{ matrix.name }}